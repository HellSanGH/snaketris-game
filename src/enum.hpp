#ifndef ENUM_HPP
#define ENUM_HPP




enum Food {
    None,
    Red,
    Green,
    Blue,
    Star
};

enum Direction {
    NORTH,
    SOUTH,
    WEST,
    EAST
};

enum Statut {
    Begin,
    Play,
    Pause,
    Win,
    GameOver
};

#endif
